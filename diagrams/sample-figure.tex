% Sample figure: a small block diagram exercising the RTL/LTR/math mix —
% Farsi node labels, an English acronym, inline math, and a MULTI-LINE
% text box (the case the old toolchain reversed). Build with:
%   python tools/build_diagrams.py 'sample-figure'
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\usepackage{amsmath}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds}
\begin{document}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    box/.style={draw, rounded corners=2pt, minimum width=26mm, minimum height=14mm, align=center}]

  % NOTE: inside a .tex figure, English/math are LTR automatically via babel
  % bidi=basic — write them plainly (do NOT use Pandoc's [...]{.ltr}, which is
  % markdown for the .qmd prose, not LaTeX).
  \node[box, fill=blue!6]   (in)  at (0,0)   {سیگنالِ ورودی\\$x(t)$};
  \node[box, fill=orange!8] (proc) at (4.2,0) {پردازشگر\\(فیلترِ FIR)};
  \node[box, fill=green!7]  (out) at (8.4,0) {سیگنالِ خروجی\\$y(t)$};

  \draw[->, thick] (in)   -- (proc);
  \draw[->, thick] (proc) -- (out);

  % a multi-line annotation box: the exact case the old XeLaTeX+bidi route
  % reversed (first line LTR). Under babel it reads correctly right-to-left.
  \node[draw, rounded corners=2pt, fill=black!3, align=right, text width=82mm,
        font=\footnotesize] at (4.2,-2.6)
       {در حوزهٔ بسامد، رابطهٔ ورودی-خروجیِ فیلتر چنین است: \mbox{$Y(f)=H(f)\,X(f)$}.\\[3pt]
        بهرهٔ باندِ عبور نزدیکِ صفر دسیبل و میرایی در باندِ توقف بالاست.};

\end{tikzpicture}}
\end{document}
